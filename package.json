{
  "name": "csa-evaluation-calculator",
  "private": true,
  "version": "1.16.0",
  "description": "CSA Evaluation Calculator — static PWA, no build step. package.json exists only to run the calc.js unit tests.",
  "scripts": {
    "test": "node --test"
  }
}
